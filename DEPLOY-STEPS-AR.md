# خطوات نشر V7

1. افتح **Supabase > SQL Editor**.
2. شغّل كامل ملف `supabase-patch-v4.sql` مرة واحدة بعد Patch الخاص بـ V6، وتأكد من ظهور Success.
3. استبدل ملفات المشروع في نسخة Git المحلية بملفات V7 مع الإبقاء على مجلد `.git`.
4. ارفع النسخة:

```bash
git add -A
git commit -m "V7 editable requests items dates and images"
git push origin main
```

5. بعد النشر اختبر طلب شراء موجود:
   - افتح التفاصيل.
   - وسّع بندًا واضغط **تعديل كل بيانات البند**.
   - عدّل المواصفات أو سعر آخر إدخال وأضف تاريخ آخر إدخال.
   - أضف بندًا جديدًا من زر **＋ إضافة بند**.
   - تأكد أن السعر النهائي للطلب تغيّر تلقائيًا.
6. اختبر زر **إضافة صور للطلب / أمر التشغيل** وأضف أكثر من صورة ثم أعد فتح الطلب وتأكد أن الصور القديمة والجديدة ظاهرة.
7. اختبر زر **تعديل بيانات الطلب** واحفظ تغييرًا في المورد أو الحالة أو تاريخ الطلب.
