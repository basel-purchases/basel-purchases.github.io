# خطوات نشر V9.1

## إذا كنت قد نفذت V9 سابقًا

1. افتح **Supabase > SQL Editor**.
2. شغّل `supabase-patch-v7.sql` مرة واحدة.
3. ارفع ملفات V9.1 إلى نفس الريبو واستبدل الملفات القديمة.
4. نفّذ:

```bash
git add -A
git commit -m "V9.1 optional quick request fields and mobile cache busting"
git push origin main
```

5. بعد نشر GitHub Pages افتح الموقع على الهاتف.
6. إذا ظهر الشكل القديم، أضف مؤقتًا إلى آخر رابط الموقع:

`?v=91`

مثال: إذا كان الرابط ينتهي بـ `/purchase-manager/` يصبح `/purchase-manager/?v=91`.

7. سجّل الدخول وافتح **طلبات جديدة**.
8. جرّب حفظ طلب يحتوي على معلومة واحدة فقط، مثل الاسم فقط أو التفاصيل فقط أو مكان متابعة الورقيات فقط أو صورة فقط.
9. جرّب أيضًا طلبًا بلا صورة، ثم أضف صورة لاحقًا واحذفها للتأكد من إمكانية بقاء الطلب بلا صور.

## إذا لم تكن قد نفذت V9 أصلًا

شغّل ملف V9 المحدث `supabase-patch-v6.sql` بعد V8، ثم ارفع ملفات V9.1. لا تحتاج `supabase-patch-v7.sql` في التثبيت الجديد لأن التعديل موجود أصلًا داخل V9 المحدث.

## الكاميرا

خيار `capture="environment"` موجود في حقول الكاميرا. الهاتف والمتصفح يقرران هل تفتح الكاميرا الخلفية مباشرة أو يظهر منتقي الصور/الكاميرا.
