# خطوات نشر V9

1. افتح **Supabase > SQL Editor** على نفس قاعدة بيانات V8.
2. شغّل كامل ملف `supabase-patch-v6.sql` مرة واحدة، بعد أن يكون Patch V8 منفذًا مسبقًا.
3. تأكد أن التنفيذ انتهى بـ **Success** بدون أخطاء.
4. استبدل ملفات المشروع في نسخة Git المحلية بملفات V9 مع الإبقاء على مجلد `.git` وملف إعداد Supabase الصحيح.
5. ارفع النسخة:

```bash
git add -A
git commit -m "V9 quick requests cards and camera capture"
git push origin main
```

6. بعد اكتمال النشر، سجّل الدخول وافتح تبويب **طلبات جديدة**.
7. اختبر إنشاء طلب جديد بإدخال **مكان الوجود + صورة فقط** ثم احفظه.
8. تأكد أن الكارد يعرض:
   - «طلب بلا اسم» فوق الصورة.
   - الصورة.
   - «الجهة غير محددة» ومكان الوجود تحتها.
9. اضغط / انقر مرتين على الكارد، ثم أضف الاسم والجهة والتفاصيل واحفظ.
10. اختبر إضافة صورة جديدة من **اختيار صور**، ثم اختبر **فتح الكاميرا** من هاتف يدعم ذلك.
11. اختبر الكاميرا كذلك في الصور القديمة: صور الطلب، صور البنود، وإضافة صور من شاشة التفاصيل.

## ملاحظة مهمة عن الكاميرا

فتح الكاميرا المباشر يعتمد على دعم الهاتف والمتصفح لـ `capture="environment"`. إذا لم يدعمه الجهاز فسيظهر اختيار ملف/صورة بدلًا منه، ويبقى رفع الصور العادي يعمل دائمًا.
