# خطوات نشر V9.2

## الترقية من V9.1 الحالية

1. افتح **Supabase > SQL Editor**.
2. شغّل ملف:

   `basel-purchases-v9.2-free-request-reference-patch.sql`

   مرة واحدة فقط.
3. ارفع ملفات V9.2 إلى نفس الريبو واستبدل ملفات V9.1 القديمة.
4. إذا كنت تستخدم Git من جهازك:

```bash
git add -A
git commit -m "V9.2 bottom item add decimal prices free request reference priority button"
git push origin main
```

5. بعد اكتمال نشر GitHub Pages افتح الموقع من جديد.
6. على الهاتف يمكنك فتح الرابط مؤقتًا مع `?v=92` في النهاية لإجبار تحميل الصفحة الجديدة، مثل:

   `/purchase-manager/?v=92`

## اختبارات سريعة بعد النشر

- أنشئ أو عدّل رقم إضبارة بالقيمة `171-172` وتأكد أنه يُحفظ.
- أدخل سعرًا مثل `25.5` أو `0.3` وتأكد أن الإجمالي يحسب ويحفظ.
- افتح طلب شراء طويل وتأكد أن زر **إضافة بند جديد** موجود في الأسفل، وأن نموذج البند الجديد يفتح بعد آخر بند.
- من قائمة الطلبات اضغط **↑** على بطاقة في الوسط وتأكد أنها تنتقل فورًا إلى أول القائمة.
- حدّث الصفحة وتأكد أن البطاقة بقيت في الأعلى بعد القراءة من Supabase.

## ملاحظة

إذا لم تنفذ Patch V9.2 على Supabase فستبقى الدوال القديمة ترفض `171-172` حتى لو كانت الواجهة الجديدة تسمح بكتابته.
