# خطوات نشر V8

1. افتح **Supabase > SQL Editor**.
2. شغّل كامل ملف `supabase-patch-v5.sql` مرة واحدة بعد Patch الخاص بـ V7، وتأكد من ظهور Success.
3. استبدل ملفات المشروع في نسخة Git المحلية بملفات V8 مع الإبقاء على مجلد `.git`.
4. ارفع النسخة:

```bash
git add -A
git commit -m "V8 manual request numbers image delete and item UX"
git push origin main
```

5. اختبر طلب شراء موجود:
   - تأكد أن المواصفات تظهر تحت اسم كل بند.
   - افتح بندًا وعدّله ثم احفظ؛ يجب أن يعود السكرول إلى نفس البند.
   - احذف صورة من صور البند وتأكد من اختفائها بعد التحديث.
6. اختبر طلبًا أو أمر تشغيل موجودًا واحذف صورة من صور الطلب.
7. أنشئ طلبًا جديدًا وأدخل رقمًا من 3 أرقام أو أكثر، مثل `123` أو `1234` أو رقم أطول.
8. من **تعديل بيانات الطلب** غيّر رقم طلب موجود إلى رقم آخر غير مستخدم من 3 أرقام أو أكثر.
